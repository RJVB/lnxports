diff --git a/check-deps/Makefile b/check-deps/Makefile
index 61e0734b..20248497 100644
--- a/check-deps/Makefile
+++ b/check-deps/Makefile
@@ -38,7 +38,7 @@ endif
 
 CFLAGS_cc_has_mfentry = -mfentry
 LDFLAGS_cxa_demangle = -lstdc++
-LDFLAGS_have_libelf = -lelf
+LDFLAGS_have_libelf = @PREFIX@/lib/elfutils/libelf.so
 CFLAGS_cc_has_mno_sse2 = -mno-sse -mno-sse2
 LDFLAGS_have_libpython2.7 = -lpython2.7
 CFLAGS_have_libpython3 = $(shell pkg-config python3$(EMBED) --cflags 2> /dev/null)
@@ -48,7 +48,7 @@ LDFLAGS_have_libluajit = $(shell pkg-config --libs  luajit 2> /dev/null)
 CFLAGS_have_libncurses  = $(shell pkg-config --cflags ncursesw 2> /dev/null)
 LDFLAGS_have_libncurses = $(shell pkg-config --libs   ncursesw 2> /dev/null)
 CFLAGS_have_libdw  = $(shell pkg-config --cflags libdw 2> /dev/null)
-LDFLAGS_have_libdw = $(shell pkg-config --libs   libdw 2> /dev/null || echo "-ldw")
+LDFLAGS_have_libdw = $(shell pkg-config --libs   libdw 2> /dev/null || echo "@PREFIX@/lib/elfutils/libdw.so")
 CFLAGS_have_libcapstone  = $(shell pkg-config --cflags capstone 2> /dev/null)
 LDFLAGS_have_libcapstone = $(shell pkg-config --libs   capstone 2> /dev/null)
 CFLAGS_have_libunwind  = $(shell pkg-config --cflags libunwind 2> /dev/null)
diff --git a/check-deps/Makefile.check b/check-deps/Makefile.check
index cb0d1e16..97923bae 100644
--- a/check-deps/Makefile.check
+++ b/check-deps/Makefile.check
@@ -58,13 +58,13 @@ endif
 
 ifneq ($(wildcard $(objdir)/check-deps/have_libelf),)
   COMMON_CFLAGS  += -DHAVE_LIBELF
-  COMMON_LDFLAGS += -lelf
+  COMMON_LDFLAGS += @PREFIX@/lib/elfutils/libelf.so
 endif
 
 ifneq ($(wildcard $(objdir)/check-deps/have_libdw),)
   COMMON_CFLAGS  += -DHAVE_LIBDW
   COMMON_CFLAGS  += $(shell pkg-config --cflags libdw 2> /dev/null)
-  COMMON_LDFLAGS += $(shell pkg-config --libs libdw 2> /dev/null || echo "-ldw")
+  COMMON_LDFLAGS += $(shell pkg-config --libs libdw 2> /dev/null || echo "@PREFIX@/lib/elfutils/libdw.so")
 endif
 
 ifneq ($(wildcard $(objdir)/check-deps/have_libunwind),)
